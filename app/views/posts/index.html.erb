<% @posts.order(created_at: :desc).each do |post| %>
  <p><%= url_checker(post.message) %> Posted on <%= post.created_at.strftime("%d/%m/%Y")%> at <%= post.created_at.strftime("%H:%M")%></p>
<% end %>

<%= link_to new_post_path do %>
  New post
<% end %>
